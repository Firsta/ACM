#include<cstdio>
int a[1001];
int main()
{
	int n;
	scanf("%d",&n);
	for(int i=0;i<n;i++)
		scanf("%d",&a[i]);
	bool flag=true;
	for(int i=0;i<n-1;i++)
		if(a[i]+a[i+1]==0)
			flag=false;
	if(flag)
		printf("UNIQUE\n");
	else
		printf("NOT UNIQUE\n");
	return 0;
}
