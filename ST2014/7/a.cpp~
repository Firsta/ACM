#include<iostream>
#include<cstdio>
#include<cstring>
#include<cstdlib>
#include<climits>
#include<algorithm>
#include<vector>
#include<stack>
#include<set>
#include<queue>
#include<map>
#include<cmath>
#include<sstream>
#include<string>

typedef long long ll;
using namespace std;

int prime[9]={
	2,3,5,7,11,13,17,19,23
};

void dfs(string s,int last,int cnt){
	for()
}

int main(){
	int t;
	scanf("%d",&t);
	while(t--){
		string s;
		cin>>s;
		int a=s[0]-'0';
		s=s.substr(1);
		dfs(s,0);
	}
	return 0;
}
