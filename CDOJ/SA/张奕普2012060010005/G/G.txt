    非非非非非常经典的八数码问题。
    鄙人不才只到达八数码的第三境界——bfs+康托展开+打表。启发式搜索的一些细节还没想明白……
    目标状态只有一个就是123456780，而能到达目标状态的状态数也是固定的，所以一开始就bfs，把目标状态到当前状态的值存进以状态hash值为下标的dist数组，因为是排列所以可以用康托展开hash。奇怪的是排列数应该是9！=362880，但我把数组大小设为400000过不了，设成1000000才过得了，所以一开始RE于不知名原因。
    每输入一个起始状态就可以通过它的hash值直接查dist数组，查询操作位O（1）。