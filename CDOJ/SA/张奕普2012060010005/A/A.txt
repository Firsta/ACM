    典型的迷宫问题，用bfs算法做。唯一的难点是对传送门的处理。因为到达一个传送门会被强制传送到传送门的另一头，所以在bfs过程中遇到传送门时可直接把传送门的另一端入队，但终点门不应被标记（一个传送门至多可以被经过两次，一次出一次入）。转移的方式为上下左右四个方向，而状态数与迷宫大小是相等的，所以复杂度是O(4*N*M)=O(N*M)，空间复杂度也是O(N*M)。数据量较小，所以在bfs过程中遇到传送门再搜索迷宫中的另一个传送门（而不是存储门对）也不会超时。