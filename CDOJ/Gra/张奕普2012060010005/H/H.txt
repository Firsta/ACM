    二维最短路径，多出的一维就是步数，即用d[t][k]存到k步到t点的最短路径。问题是K次以上这个条件，意味着k的范围是不可知的，这就要想出一个方法把大于k的步数的结果归结到k里，考虑dijstra的优先队列优化，可以每次都取d最小的一个状态进行更新，取出的状态是到终点的话就可以直接输出答案了，这样也有点bfs的感觉吧……
    如果队列为空后d[t][k]为INF的话就是不可达了，可以直接输出-1.
    考虑用了优先队列优化的dijstra，时间复杂度为O(k*n*log(k*n))。